'use client';

// Native scrolling - no smooth scroll library
// Users can scroll freely with default browser behavior
export default function SmoothScroll({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
