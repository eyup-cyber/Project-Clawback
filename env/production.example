# Production environment template
# Copy this file to .env.production.local and fill in your production values
# ⚠️ NEVER commit actual production secrets to version control!

# ============================================================================
# CORE CONFIGURATION
# ============================================================================
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://scroungers.co
NEXT_PUBLIC_APP_NAME=Scroungers Multimedia

# ============================================================================
# SUPABASE (Required)
# ============================================================================
# Get these values from: https://app.supabase.com → Your Production Project → Settings → API
# - Project URL → NEXT_PUBLIC_SUPABASE_URL (format: https://xxxxx.supabase.co)
# - anon/public key → NEXT_PUBLIC_SUPABASE_ANON_KEY (safe for client-side use)
# - service_role key → SUPABASE_SERVICE_ROLE_KEY (SERVER-SIDE ONLY - never expose to browser!)
# ⚠️ The service_role key has admin access - protect it carefully!
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ============================================================================
# CLOUDFLARE R2 STORAGE (Required for media uploads in production)
# ============================================================================
# Configure in Cloudflare Dashboard → R2 → Create Production Bucket
# Create API tokens with R2 read/write permissions: Cloudflare Dashboard → My Profile → API Tokens
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=scroungers-media-prod
R2_PUBLIC_URL=https://media.scroungers.co

# ============================================================================
# EMAIL SERVICE (Resend) - Required for production
# ============================================================================
# Get your API key from: https://resend.com/api-keys
# Verify your domain in Resend Dashboard → Domains
RESEND_API_KEY=
FROM_EMAIL="Scroungers <hello@scroungers.co>"
ADMIN_EMAIL=admin@scroungers.co

# ============================================================================
# REDIS CACHE (Recommended for production)
# ============================================================================
# Use managed Redis service (Upstash, Redis Cloud, etc.) or self-hosted
# Format: redis://user:password@host:port or rediss:// for TLS
REDIS_URL=

# ============================================================================
# SECURITY KEYS (Required)
# ============================================================================
# Generate secure random strings (never reuse from development!):
#   CSRF_SECRET: openssl rand -hex 16 (minimum 32 characters)
#   JWT_SECRET: openssl rand -hex 32 (minimum 64 characters)
# ⚠️ Rotate these periodically and never share them!
CSRF_SECRET=change_me_32_chars_min
JWT_SECRET=change_me_64_chars_min

# Monitoring / Analytics (optional)
SENTRY_DSN=
LOGROCKET_APP_ID=
NEXT_PUBLIC_GA_ID=
PLAUSIBLE_DOMAIN=scroungers.co

# Feature flags / logging
FEATURE_EMAIL_NOTIFICATIONS=true
FEATURE_REDIS_CACHE=true
FEATURE_MONITORING=true
RATE_LIMIT_ENABLED=false
LOG_LEVEL=info

